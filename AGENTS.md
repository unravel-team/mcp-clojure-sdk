# Agent Preferences

- Create a commit after small, contained changes.
- Write or update a test for each change when feasible.
- Run `make check`, `make format`, and `make test` after making changes.
- Prefer detached HEAD workflows; commits can be created directly without branching.
- Ask for confirmation if approach or scope needs to change.
