(ns io.modelcontext.cljc-sdk-test
  (:require [clojure.test :refer [deftest testing is]]
            #_:clj-kondo/ignore
            [io.modelcontext.cljc-sdk :as sut]))

(deftest a-test (testing "FIXME, I fail." (is (= 0 1))))
